<UserControl x:Class="CabarlesWPF.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#667eea" Offset="0.0"/>
                <GradientStop Color="#764ba2" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Logo/Icon Circle -->
            <Border Width="120" Height="120" 
                    Background="White" 
                    CornerRadius="60" 
                    Margin="0,0,0,30"
                    HorizontalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.3" Color="Black"/>
                </Border.Effect>
                <Viewbox Margin="25">
                    <TextBlock Text="📚" FontSize="60"/>
                </Viewbox>
            </Border>
            
            <!-- Title -->
            <TextBlock Text="Student Management System" 
                       FontSize="42" 
                       FontWeight="Bold" 
                       Foreground="White" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.5" Color="Black"/>
                </TextBlock.Effect>
            </TextBlock>
            
            <!-- Subtitle -->
            <TextBlock Text="Manage your students efficiently" 
                       FontSize="18" 
                       Foreground="#E0E0E0" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,40"/>
            
            <!-- Button -->
            <Button Command="{Binding NavigateToStudentManagementCommand}"
                    Width="280" 
                    Height="65" 
                    FontSize="20"
                    FontWeight="SemiBold"
                    Background="White"
                    Foreground="#667eea"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="32.5"
                                            Padding="30,18">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.3" Color="Black"/>
                                        </Border.Effect>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <TextBlock Text="🎓 " FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Content" Value="Manage Students"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#667eea" Offset="0.0"/>
                                            <GradientStop Color="#764ba2" Offset="1.0"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
        
        <!-- Footer -->
        <TextBlock Grid.Row="2" 
                   Text="CabarlesIPT102 © 2026" 
                   FontSize="14" 
                   Foreground="#E0E0E0" 
                   HorizontalAlignment="Center"
                   VerticalAlignment="Bottom"
                   Margin="0,0,0,30"
                   Opacity="0.7"/>
    </Grid>
</UserControl>
